import { describe, expect, it } from 'vitest';
import { CdsService } from './cds.service';

describe('CdsService', () => {
  it('should be defined', () => {
    expect(CdsService).toBeDefined();
  });
});
